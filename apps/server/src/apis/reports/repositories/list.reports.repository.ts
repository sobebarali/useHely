/**
 * List reports repository
 *
 * Database operations for GET /api/reports
 * This endpoint doesn't need database access - returns static metadata
 */

// No database operations needed for listing available report types
// Report metadata is defined in reports.constants.ts
