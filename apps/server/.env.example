DATABASE_URL=
BACKEND_URL=

# CORS Configuration (REQUIRED in production)
# Specify the allowed origin for cross-origin requests
# Example: https://yourdomain.com or http://localhost:3000
# In production, this MUST be set to prevent CSRF attacks
# In development/test, defaults to http://localhost:3000 if not set
CORS_ORIGIN=

UPSTASH_REDIS_URL=
UPSTASH_REDIS_TOKEN=

# Encryption
# Generate with: openssl rand -hex 32
# Or using Node.js crypto: require('crypto').randomBytes(32).toString('hex')
ENCRYPTION_MASTER_KEY=

RESEND_API_KEY=
EMAIL_FROM=
EMAIL_FROM_NAME=